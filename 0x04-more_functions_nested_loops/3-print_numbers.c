#include "main.h"

/**
 * print_numbers - prints numbers between 0 to 9.
 * Return: no return.
 */

void print_numbers(void)
{
		int pr;

		for (pr = 48; pr < 58; pr++)
			{
			_putchar(pr);
			}
			_putchar('\n');
}

